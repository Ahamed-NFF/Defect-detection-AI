# Few-shot ablation: 10 real defects, NO synthetic (the "without GAN" arm).
# Traditional aug on so the only difference vs *_ours is the synthetic data. (Member 3)
run_name: "bottle_fewshot10"
category: "bottle"
backbone: "resnet50"
traditional_aug: true
use_synthetic: false
few_shot_n: 10
epochs: 30
batch_size: 32
lr: 1.0e-4
