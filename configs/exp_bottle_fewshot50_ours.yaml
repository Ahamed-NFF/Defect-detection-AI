# Few-shot ablation: 50 real defects + diffusion-synthesized defects (the
# "with GAN" arm — benefit should be smallest here as real data grows). (Member 3)
run_name: "bottle_fewshot50_ours"
category: "bottle"
backbone: "resnet50"
traditional_aug: true
use_synthetic: true
synthetic_dir: "data/synthetic/bottle"
synthetic_n: "balance"   # fill defects up to the good count (so few_shot_n matters)
few_shot_n: 50
epochs: 30
batch_size: 32
lr: 1.0e-4
