# Few-shot ablation: 10 real defects + diffusion-synthesized defects (the
# "with GAN" arm — expected to help most at this extreme). (Member 3)
run_name: "bottle_fewshot10_ours"
category: "bottle"
backbone: "resnet50"
traditional_aug: true
use_synthetic: true
synthetic_dir: "data/synthetic/bottle"
few_shot_n: 10
epochs: 30
batch_size: 32
lr: 1.0e-4
