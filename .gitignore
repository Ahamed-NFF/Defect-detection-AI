# Data & model artifacts (too big for git)
data/raw/
data/processed/
data/synthetic/
experiments/checkpoints/
*.ckpt
*.safetensors
*.pkl

# NOTE: results JSON, the comparison table, FID files, and report figures are
# intentionally tracked (small) so they sync between the GPU and CPU machines
# for report writing. The big artifacts above stay out of git.

# LaTeX build artifacts (report.tex is the tracked source)
reports/*.aux
reports/*.log
reports/*.out
reports/*.toc
reports/*.pdf

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Node (frontend)
frontend/node_modules/
frontend/.next/

# OS / editor
.DS_Store
.vscode/
.idea/
.claude/
