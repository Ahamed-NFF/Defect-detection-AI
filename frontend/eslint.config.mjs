// ESLint is intentionally minimal for the demo skeleton (kept compatible with
// Next 15 / ESLint 9 flat config without extra dependencies). Tighten later by
// extending "next/core-web-vitals" via FlatCompat if desired.
const eslintConfig = [];

export default eslintConfig;
